const Task = (focus, status, description, startDate, dueDate, projectName, taskIndex) => ({
  focus,
  status,
  description,
  startDate,
  dueDate,
  projectName,
  taskIndex,
});

export default Task;
