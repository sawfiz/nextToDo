const Task = (focus, status, description, startDate, dueDate, projectIndex, taskIndex) => ({
  focus,
  status,
  description,
  startDate,
  dueDate,
  projectIndex,
  taskIndex,
});

export default Task;
